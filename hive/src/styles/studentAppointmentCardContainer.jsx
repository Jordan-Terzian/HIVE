import styled from 'styled-components';

const StudentAppointmentCardContainter = styled.div`
  margin-bottom: 15px;
  margin-right: 15px; // added margin right for separation

`;

export default StudentAppointmentCardContainter;
